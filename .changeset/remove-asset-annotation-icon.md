---
"@skip-go/widget": patch
---

Remove the warning icon from asset annotations. The swap page now shows only the `assetAnnotations` badge (no icon), and the token selector shows only the description line and border highlight. Badge/border/description colors and `selector.pinToTop` are unchanged.
