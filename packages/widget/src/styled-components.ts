// Re-export styled-components to ensure the same instance is used across the monorepo
export {
  styled,
  css,
  keyframes,
  ThemeProvider,
  StyleSheetManager,
  useTheme,
} from "styled-components";
export type { IStyledComponent } from "styled-components";
