import { Modals } from "@/modals/registerModals";

// We don't need to register modals in the explorer app
// The widget package handles all modal registration
// Just export the Modals enum for reference

export { Modals };
